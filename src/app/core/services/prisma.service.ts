// This service has been removed because Prisma Client cannot run in browser environments
// For database operations in Angular, use HTTP services that call backend APIs
// Or use Supabase client for direct database access from the frontend

export class PrismaService {
  constructor() {
    throw new Error('PrismaService is not available in browser environments. Use HTTP services or Supabase instead.');
  }
}
